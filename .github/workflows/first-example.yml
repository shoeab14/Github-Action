name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Welcome message
        run: echo "My first github action job"

      - name: List files
        run: ls -R
          
      - name: Read file
        run: cat .github/workflows/demo.txt

      - name: make dir empty
        run: mkdir demo01
        
